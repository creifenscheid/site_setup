lib.contentTemplate < lib.boilerplates.fluidtemplate
lib.contentTemplate {
    templateName = {$plugin.tx_sitsetup.view.templateName}
}

lib.contentRendering < styles.content.get
lib.contentTendering {
    current = 1
    select.where.wrap = {#colpos}=|
}