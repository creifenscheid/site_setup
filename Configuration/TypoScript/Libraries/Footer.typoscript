lib.footer < lib.boilerplates.fluidtemplate
lib.footer {
    templateName = {$plugin.tx_sitsetup.view.footerTemplateName}
    
    dataProcessing {
        10 < lib.boilerplates.menu
        10 {
            if.isTrue = {$plugin.tx_sitsetup.view.footerMenuParentPageId}
            special.value = {$plugin.tx_sitsetup.view.footerMenuParentPageId}
        }
    }
}