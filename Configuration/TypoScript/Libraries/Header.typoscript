lib.headerTemplate < lib.boilerplates.fluidtemplate
lib.headerTemplate {
    if.isTrue = {$plugin.tx_sitesetup.view.headerTemplateName}
    templateName = {$plugin.tx_sitesetup.view.headerTemplateName}
    
    dataProcessing {
        10 < lib.boilerplates.fileProcessor
		10 {
		    if.isTrue = {$plugin.tx_sitesetup.view.menuParentPageId}
		    special.value = {$plugin.tx_sitesetup.view.menuParentPageId}
		}
    }
}