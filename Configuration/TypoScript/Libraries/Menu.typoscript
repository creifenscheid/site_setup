lib.menuTemplate < lib.boilerplates.fluidtemplate
lib.menuTemplate {
    templateName = {$plugin.tx_sitsetup.view.menuTemplateName}
    
    dataProcessing {
        10 < lib.boilerplates.menu
        10 {
            if.isTrue = {$plugin.tx_sitsetup.view.menuParentPageId}
            special.value = {$plugin.tx_sitsetup.view.menuParentPageId}
        }
    }
}